Sources for the plugin `plugin.androidPreview`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "plugin.androidPreview" = {
            publisherId = "tech.scotth",
        },
    },
}
```
